export type VideoInfo = {
  ip: string;
  path: string;
  kbps: number;
  codec: string;
  psnr: number;
  psnr_max: number;
  psnr_min: number;
  ssim: number;
  ssim_max: number;
  ssim_min: number;
};

export type WebRequest = {
  
}
